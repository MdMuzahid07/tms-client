# tms-client

## Environment

By default the client calls `http://localhost:5000/api`.

To override, create `tms-client/.env.local` (or copy from `tms-client/.env.example`):

```bash
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```
